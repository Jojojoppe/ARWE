{
  "tasks": [
    {
      "label": "Build",
      "group": "build",
      "type": "shell",
      "command" : "if [ ! -d build ] ; then mkdir build ; cd build ; cmake .. ; cd .. ; fi ; cd build ; make"
    }
  ]
}